self.addEventListener('install', function(event) {
    // Gérer le pré-chargement des ressources si nécessaire
});

self.addEventListener('fetch', function(event) {
    // Gérer les requêtes et utiliser le cache si nécessaire
});
