{% extends 'base.html.twig' %}

{% block title %}<h1>Ajouter un rendez-vous</h1>{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Ajouter un rendez-vous</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
